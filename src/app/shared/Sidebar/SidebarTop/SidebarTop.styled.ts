import styled from "styled-components";

export const Container = styled.div`
    width: 100%;
    height: 4rem;
    position: fixed;
    top: 0;
    right: 0;
    left: 0;
    background-image: linear-gradient(to right, #000 10%, var(--blue-main) 90%);
    z-index: 99;
`;